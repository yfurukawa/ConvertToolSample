/*!-----------------------------------------------------------------------------
@file       Observer.h
@brief      ObserverパターンのObserverクラス
@attention  なし
------------------------------------------------------------------------------*/
#pragma once

// インクルードファイル ======================================================

// クラスの前方宣言 =======================================================
class AbstructSubject;

// マクロの宣言 ================================================================

// 型の宣言 ==================================================================


// クラス定義 ==================================================================
class Observer
{
public:
    Observer(void);
    virtual ~Observer(void);
    virtual void notify(AbstructSubject* subject) = 0;
};

